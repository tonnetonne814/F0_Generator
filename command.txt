docker compose build --no-cache 
docker compose up -d
docker exec -it {container_name} /bin/bash
poetry install 
source .venv/bin/activate

# パッケージ追加時
poetry add <package_name>で追加すれば、自動的に依存関係もpyproject.tomlに追加される

